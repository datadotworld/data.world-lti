version: '3'
services:
  canvas-web:
    image: lbjay/canvas-docker
    container_name: canvas-lms
    ports:
      - 8000:3000
  lti-web:
    build: .
    container_name: lti-web
    ports:
      - 8080:3000